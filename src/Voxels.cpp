#include "Voxels.hpp"

Voxels::Voxels() : u(), v(), w(),
                   u0(), v0(), w0(),
                   avg_u(), avg_v(), avg_w(),
                   omg_x(), omg_y(), omg_z(), omg_length(),
                   dens(), temp(),
                   fx(), fy(), fz()
{
}

Voxels::~Voxels()
{
}